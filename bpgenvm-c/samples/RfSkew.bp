module RfSkew

  import Core   // V6: los tipos de Core se importan

  function Main()
    try
      var s: string := readFile("/no/existe/jamas.txt")
      print "leido: " + s
    catch e: RuntimeError
      print "msg: " + e.msg
    endtry
  end Main
end RfSkew
